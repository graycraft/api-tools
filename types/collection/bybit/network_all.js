/**
 * @typedef {import("#types/bybit.ts").coinChain} assetChain
 * @typedef {import("#types/bybit.ts").coinName} assetCode
 * @typedef default
 * @type {{
 *   chain: assetCode;
 *   chainType: assetChain;
 * }[]}
 */
