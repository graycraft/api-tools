/**
 * @typedef default
 * @prop {"candles" | "level2" | "market_trades" | "status" | "ticker" | "ticker_batch" | "user"} channel
 * @prop {string} [jwt]
 * @prop {Array<string>} [product_ids]
 * @prop {"subscribe" | "unsubscribe"} type
 */
